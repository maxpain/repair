<p id="notice"><%= notice %></p>


  <h1><%= @tiding.title %></h1>

<div class="col-xs-8">
  <p>
    <strong>Description:</strong>
    <%= @tiding.description %>
  </p>
</div>
  <%= image_tag @tiding.image_url.small.url %><br>

<% if user_signed_in? %>
<% if current_user.admin? %>  
  <%= link_to 'Edit', edit_tiding_path(@tiding) %>
<% end %>
<% end %>
<%= link_to 'Back', welcome_index_path %>
